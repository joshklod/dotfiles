[pretty]
	twoline  = tformat:%C(auto)%h %C(auto,blue)%<(16)%an%C(auto)%d%n  %s
	twolineg = tformat:\t%C(auto)%h %C(auto,blue)%<(16)%an%C(auto)%d%n\t  %s

[alias]
	# Not meant to be used
	log2  = !":; git log --decorate --abbrev-commit"
	log3  = !"git log2 --pretty=twoline"
	log3g = !"git log3 --pretty=twolineg --graph"

	# Log shortcuts
	llog = !git log2 --author-date-order --show-linear-break
	logp = !git log2 --patch --reverse
	logg = !git log2 --graph
	loga = !git log2 --graph --all
	lgg  = !git log3 --author-date-order --show-linear-break
	lgp  = !git log3 --patch --reverse
	lg   = !git log3g
	lga  = !git log3g --all
	lgs  = !git log3g --all --simplify-by-decoration

	# Other shortcuts
	stl   = !"git status && git lgs"
	diffs = diff --stat
	ctags = !~/.config/git/scripts/ctags.sh
	cptree = !~/.config/git/scripts/cptree.sh

[pager]
	log3  = less -S
	logp  = less -Sx 1,5
	lgp   = less -Sx 1,5
	lgs   = less -FRX
	diffs = less -FRX

# vim: ft=gitconfig
